问题：
    1.算法的时间复杂度统计的不是时间，而是语句的执行次数？
        机器的配置不同，统计时间不合适，还是统计次数吧。
    2.为什么开始要讲线性表？
        从一个例子开始的。表中的记录为一个数据元素，从上到下，表中的记录构成了线性关系。
        线性表采用顺序存储，叫顺序表；采用链式存储，叫链表；采用散列存储，叫散列表；采用索引存储，叫索引表。
        对链表的操作进行限制，就产生了队列 栈 串等新的数据结构。
    3.头节点不是首节点！
        头节点的类型也是链表的节点类型
        构造链表的函数返回链表的头节点就可以了。